<!DOCTYPE html>
<html lang="fr" dir="ltr">

<%- include('head'); %>

<body>
  <% if (!firstUser) { %>
    <h1>S'identifier</h1>
    <% if (messages.error) { %>
      <p><%=messages.error %></p>
    <% } %>
    <form action="/login" method="post">
      <label>
        user
        <input class="input" type="text" name="name" placeholder="Nom d'utilisateur">
      </label>
      <label>
        password
        <input class="input" type="text" name="password" placeholder="Insérez le mot magique">
      </label>
      <button type="submit">Valider</button>
    </form>
  <% } else { %>
    <%- include('register',  {registerClass: 'flex', locations: locations}); %>
  <% } %>
  <%- include('scripts'); %>
</body>

</html>
